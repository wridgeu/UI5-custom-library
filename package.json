{
  "name": "libraryconsumption",
  "version": "1.0.0",
  "private": true,
  "description": "A test repository to learn how to create a small library and how to consume it",
  "scripts": {
    "ui:start": "cd packages/ui5-library-consumption/ && yarn start",
    "ui:init:start": "yarn buildLib && cd packages/ui5-library-consumption/ && yarn start",
    "ctrllib:dev": "cd packages/ui5-library/ && yarn watch",
    "ctrllib:build": "cd packages/ui5-library/ && yarn _build",
    "themelib:build": "cd packages/ui5-theme-library/ && yarn build",
    "setup": "yarn"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@openui5/ts-types": "^1.60.5"
  }
}
